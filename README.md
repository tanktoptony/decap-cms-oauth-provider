# Green Eye OAuth Helper (Netlify)

Deploy these files as a separate Netlify site. Verify `/.netlify/functions/auth` returns JSON.
Then point your main site's Decap CMS `backend.base_url` to this site's origin.
